apiVersion: v1
kind: Service
metadata:
  labels:
    app: "multicloud-operators-subscription"
  name: multicloud-operators-subscription
  annotations:
    service.alpha.openshift.io/serving-cert-secret-name: multicloud-operators-subscription  
spec:
  ports:
  - port: 8443
    protocol: TCP
    targetPort: 8443
  selector:
    operator: "multicloud-operators-hub-subscription"
  sessionAffinity: None
  type: ClusterIP
