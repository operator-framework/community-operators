query {
  repository(owner: “operator-framework”, name: “community-operators”) {
    pullRequests(first: <n_pullrequests>) {
      nodes {
        number
        author { login }
        comments(first: <n_comments>) {
          nodes {
            author { login }
          }
        }
        timelineItems(itemTypes: LABELED_EVENT, first: <n_labeledevents>) {
          nodes {
            ... on LabeledEvent {
              actor { login }
              label { name }
            }
          }
        }
      }
    }
  }
  rateLimit {
    limit
    cost
    remaining
    resetAt
  }
}

