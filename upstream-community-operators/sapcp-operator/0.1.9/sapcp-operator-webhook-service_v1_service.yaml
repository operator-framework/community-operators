apiVersion: v1
kind: Service
metadata:
  name: sapcp-operator-controller-manager-service
spec:
  ports:
    - port: 443
      protocol: TCP
      targetPort: 9443
  selector:
    control-plane: controller-manager
  sessionAffinity: None
  type: ClusterIP